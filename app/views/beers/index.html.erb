<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 30%), url('<%= image_path 'beer_5.jpeg' %>')">
      <div class="banner-content">
        <h1>Beers Room</h1>
        <div class="text-center padded">
        <%= link_to "Add your beer!", new_beer_path, class: "btn btn-primary" %>
        </div>
      </div>
      </div>

      <br>
        <div class="row">
          <% @beers.each do |beer| %>
            <div class="col-xs-12 col-sm-4 space"
            style="padding: 10px">

            <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path beer.photo, height: 200, width: 350, crop: :fill %>');">
              <div class="card-category"></div>
              <div class="card-description">
                <h2><%= beer.name %></h2>
                <p><%= beer.category %></p>
              </div>
              <%= link_to edit_beer_path(cocktail), class: 'card-link2 white' do %> <i class="fa fa-edit"></i> <% end %>
              <%= link_to beer_path(beer), method: :delete, data: { confirm: 'Are you sure?' }, class: 'card-link1 white' do %>
                  <i class='fa fa-trash'></i>
                    <% end %>
              <%= link_to "", beer_path(beer), class: 'card-link' %>
              </div>
            </div>
          <%end%>
          </div>
          <br>
